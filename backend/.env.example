# Backend Environment Variables

# Nebius AI API Key (required for prompt optimization)
NEBIUS_API_KEY=your_nebius_api_key_here

# Firebase Service Account (choose one method)
# Option 1: JSON string (for deployment like Render)
FIREBASE_SERVICE_ACCOUNT_JSON={"type": "service_account", ...}

# Option 2: Path to service account file (for local development)
FIREBASE_SERVICE_ACCOUNT_PATH=backend/services/serviceAccountKey.json
